import { FlyerNotePipe } from './flyer-note.pipe';

describe('FlyerNotePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyerNotePipe();
    expect(pipe).toBeTruthy();
  });
});
