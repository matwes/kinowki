import { FlyerKindNamePipe } from './flyer-kind-name.pipe';

describe('FlyerKindNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyerKindNamePipe();
    expect(pipe).toBeTruthy();
  });
});
