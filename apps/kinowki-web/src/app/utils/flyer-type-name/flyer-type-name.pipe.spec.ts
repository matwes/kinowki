import { FlyerTypeNamePipe } from './flyer-type-name.pipe';

describe('FlyerTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyerTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
