import { FlyerSizeNamePipe } from './flyer-size-name.pipe';

describe('FlyerSizeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyerSizeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
