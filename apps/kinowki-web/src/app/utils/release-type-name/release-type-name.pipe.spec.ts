import { ReleaseTypeNamePipe } from './release-type-name.pipe';

describe('ReleaseTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
