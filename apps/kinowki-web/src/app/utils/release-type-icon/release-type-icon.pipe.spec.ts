import { ReleaseTypeIconPipe } from './release-type-icon.pipe';

describe('ReleaseTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
